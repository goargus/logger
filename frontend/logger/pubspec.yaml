name: logger
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  auth0_flutter: ^1.6.0
  intl: ^0.20.0
  file_picker: ^8.0.0
  http: ^1.2.1
  shared_preferences: ^2.3.2
  js: ^0.6.7

dev_dependencies:
  flutter_lints: ^5.0.0
  
flutter:
  assets:
    - assets/logo.png